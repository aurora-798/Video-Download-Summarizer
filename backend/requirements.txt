fastapi>=0.115.0
uvicorn[standard]>=0.30.0
yt-dlp>=2025.1.15
pydantic>=2.0.0
# Optional: ships a prebuilt static ffmpeg binary so users don't need
# `brew install ffmpeg`. yt-dlp picks it up automatically via the
# ffmpeg_location option we wire in ffmpeg_check.py.
imageio-ffmpeg>=0.5.1

# --- AI summarizer ----------------------------------------------------
# httpx: streaming LLM client and (optional) remote Whisper API.
httpx>=0.27.0
# python-dotenv: read backend/.env for API keys at startup.
python-dotenv>=1.0.1
# faster-whisper: local CPU/GPU speech-to-text. Default Whisper backend.
# First call downloads ~70-250MB of model weights into ~/.cache/huggingface/.
# If you'd rather use a remote API (SiliconFlow / Groq / OpenAI), set
# WHISPER_BACKEND=api in .env — faster-whisper still installs but stays idle.
faster-whisper>=1.0.3
